= simple_form_for [@user, @workout] do |f|
  = f.input :name
  / = f.input :date
  = f.date_select :date, { default: Date.today}
  = f.submit
  / added :user to the simple_form_for and it worked, but I have no idea why.